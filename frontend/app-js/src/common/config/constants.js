import settings from './settings';

// eslint-disable-next-line import/prefer-default-export
export const { STORAGE_SUBSCRIPTION_THROTTLE_INTERVAL } = settings;
export const { USE_DART_AUTH } = settings;
